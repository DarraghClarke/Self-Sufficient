<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.acdos.comp41690">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".setup.SetupPagerActivity"
            android:label="ScreenSlidePagerActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".setup.WaterOnlyPagerFragment"
            android:label="WaterOnlyPagerFragment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".setup.SolarOnlyPagerFragment"
            android:label="SolarOnlyPagerFragment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".setup.FullPagerFragment"
            android:label="FullPagerFragment"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".setup.MapActivity"
            android:label="MapActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ElectricityActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".RainActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>
</manifest>